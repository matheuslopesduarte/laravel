<?php

namespace App;

interface ExampleInterface
{
    // é tipo o interface do TypeScript ou Java, ou seja, burocrático e chato
    // não utilizaremos muito isso no projeto por enquanto (ou nunca)
}
